<div class="sidebar">

  {% if settings.display-recent-posts %}
  <div class="module recent-posts first">
    <label>Recent posts</label>
    <ul class="posts">
      {% for recent_article in blogs[blog.handle].articles | limit: 5 %}
      <li class="post {% include 'for-looper' %}">
        <a class="title" href="{{ recent_article.url }}">{{ recent_article.title }}</a>
        <span class="date">{{ recent_article.published_at | date: "%b %d, %Y" }}</span>
      </li>
      {% endfor %}
    </ul>
  </div>
  {% endif %}

  {% if settings.display-tag-list and blog.all_tags.size > 0 %}
  <div class="module tags-list {% unless settings.display-recent-posts %}first{% endunless %}">
    <label>Tags</label>
    <ul class="tags">
      {% for tag in blog.all_tags %}
        {% assign tagCount = 0 %}
        <li class="tag">
          <a href="{{ blog.url }}/tagged/{{ tag | handle }}">
            {{ tag }}
            <span class="count">
              {% for article in blogs[blog.handle].articles %}
                {% for temp_tag in article.tags %}
                  {% if temp_tag == tag %}{% capture tagCount %}{{ tagCount | plus: 1 }}{% endcapture %}{% endif %}
                {% endfor %}
              {% endfor %}
              ({{ tagCount }})
            </span>
          </a>
        </li>
      {% endfor %}
      
    </ul>
  </div>
  {% endif %}

  {% if settings.display-twitter %}
  <div class="module twitter {% unless settings.display-recent-posts or settings.display-tag-list %}first{% endunless %}">
    <label>Twitter</label>
    <ul class="tweets">
      <li class="tweet">
        <p class="text">Morbi leo risus, porta ac consectetur ac, vestibulum at eros. Praesent commodo cursus magna, vel scelerisque nisl consectetur et.</p>
        <span class="date">3 Hours ago</span>
      </li>
      <li class="tweet">
        <p class="text"><a href="/">@dannyboyle</a> donec ullamcorper nulla non metus auctor fringilla. Nulla vitae elit libero, a pharetra augue.</p>
        <span class="date">6 Hours ago</span>
      </li>
      <li class="tweet">
        <p class="text">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Morbi leo risus, porta ac consectetur ac, vestibulum at eros.</p>
        <span class="date">11 Hours ago</span>
      </li>
    </ul>
    <a class="follow-us" href="http://www.twitter.com/{{ settings.twitter-username }}">Follow @twitterUsername</a>
  </div>
  {% endif %}

</div>