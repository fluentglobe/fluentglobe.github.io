include /mixins/page-helpers.jade

- googleAnalyticsSiteID = "UA-XXXXX-X"
doctype html
<!--[if IE 8 ]><html lang="en" class="no-js ie8"><![endif]-->
<!--[if (gte IE 9)|(gt IEMobile 7)|!(IEMobile)|!(IE)]><!-->
html.no-js(lang="en")
  <!--<![endif]-->
  head
    meta(charset="utf-8")
    +head-assets()
    title
      block title
        - var title = 'a b c'
        TODO common title extraction
    block head

  body
    +facebook-root()
    main
      div#content
        block content
          p Content block to be filled in
      != SocketStream
      script(type="application/config").
        //TODO declare('title'...)
        declare('title','#{title}')
        declare('entry module','/entry');


    +body-assets
    if googleAnalyticsSiteID
      script.
        var _gaq=[['_setAccount','#{googleAnalyticsSiteID}'],['_trackPageview']];
            (function(d,t){var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
            g.src='//www.google-analytics.com/ga.js';
            s.parentNode.insertBefore(g,s)}(document,'script'));

    +connection-popup()
    script.
      // kicking off the view entry.js
      //- require('/entry'); TODO inject var view entry context path

    block body-tail
